// Top-level build file placeholder (Android Studio will generate proper file)
buildscript {
    repositories { google(); mavenCentral() }
    dependencies { classpath 'com.android.tools.build:gradle:8.10.1' }
}
allprojects {
    repositories { google(); mavenCentral() }
}
